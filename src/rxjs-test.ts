import { from,of } from 'rxjs'
// import {  switchMap  } from 'rxjs/operators'

// from([1,2,3]).subscribe(number=>{console.log(number)})

// from([1,2,3]).pipe(switchMap((val:number)=> of(val))).subscribe((val:any)=>{console.log(val)})

// of(1,2,3).subscribe(val => console.log(val))